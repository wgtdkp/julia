{
    /* 
     * If a directive is specified more than once,
     * the first one will be active.
     */
    "host": "localhost",
    "port": 12345,
    "doc_root": "./www",
    "dynamic": {
        "php": {
            "server": "27.0.0.1:9000"
        }
    }
}
